## 1. 项目骨架与运行入口

- [ ] 1.1 创建 `fileglide` 包目录、`__main__.py`、`main.py`、`app.py` 和根 `click` 入口。
- [ ] 1.2 在 `pyproject.toml` 中补齐 CLI script 入口与实现所需的基础元数据。
- [ ] 1.3 建立命令分组骨架：`file`、`path`、`tree`、`text`、`inspect`、`batch`。

## 2. 统一输出与错误契约

- [ ] 2.1 定义统一 JSON 输出模型与错误模型。
- [ ] 2.2 实现命令执行器与门面层，统一处理默认 `json` 输出、warnings、errors 和 dry-run。
- [ ] 2.3 为主要危险操作定义结构化预演结果与确认字段。

## 3. 文件与路径核心能力

- [ ] 3.1 实现文件与路径创建、删除、存在性检查和作用域校验服务。
- [ ] 3.2 实现文件与路径遍历服务，支持递归、深度限制、include/exclude 过滤。
- [ ] 3.3 实现文件大小与目录聚合大小统计服务。
- [ ] 3.4 为上述能力接入 `click` 命令并补齐 JSON 返回字段。

## 4. 文本读写与精确编辑

- [ ] 4.1 实现 `EncodingService`，覆盖 UTF-8 无 BOM 优先、BOM 识别和常见编码探测。
- [ ] 4.2 实现文本整文件读取、按行读取、按区间读取和元数据返回。
- [ ] 4.3 实现文本覆盖写入、尾部追加和中部插入。
- [ ] 4.4 实现按行、按区间和按锚点的精确编辑流程。
- [ ] 4.5 实现文本写回前的编码可写性检查与保真信息回报。

## 5. 搜索、匹配与二进制能力

- [ ] 5.1 实现路径名和文件名的 exact / contains / fuzzy 搜索。
- [ ] 5.2 接入 `vortezwohl` Levenshtein 排序并返回评分元数据。
- [ ] 5.3 实现文本正则检索与结构化命中结果。
- [ ] 5.4 实现二进制读取、二进制写入、字节切片和二进制安全复制。
- [ ] 5.5 实现文本工作流与二进制工作流的自动分流与显式错误提示。

## 6. 批量执行与计划文件

- [ ] 6.1 设计并实现批量计划文件结构与加载校验逻辑。
- [ ] 6.2 实现批量 dry-run、逐步执行、逐步结果汇总和整体状态判定。
- [ ] 6.3 为批量执行增加单步失败报告、整体 warnings/errors 和可审计输出。

## 7. 测试与回归验证

- [ ] 7.1 建立 CLI 测试夹具与基础命令回归测试。
- [ ] 7.2 建立文本编码样本夹具，覆盖 UTF-8、GBK、GB18030、Shift-JIS 和 UTF-16。
- [ ] 7.3 建立二进制样本夹具，覆盖读取、写入、切片和批量执行场景。
- [ ] 7.4 为 Levenshtein 模糊匹配、正则搜索、精确编辑和批量计划执行补齐测试。
- [ ] 7.5 复查新增文件的 UTF-8 无 BOM 编码与中文输出，主动修正乱码问题。